import React from "react";
import ResultsContainer from "../components/ResultsContainer";

function Saved() {
    return(
        <main>
            <ResultsContainer />
        </main>
    );
}

export default Saved;